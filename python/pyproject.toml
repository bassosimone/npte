[project]
name = "npte"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = ["pytest", "pytest-cov"]

[build-system]
requires = ["uv_build>=0.11,<0.12"]
build-backend = "uv_build"

# Flat layout: the npte module sits next to pyproject.toml, not under src/.
[tool.uv.build-backend]
module-root = ""
